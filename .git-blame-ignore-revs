# style: apply prettier formatting + eslint import sorting
# Add the commit hash after running the initial formatting pass
# Then configure git: git config blame.ignoreRevsFile .git-blame-ignore-revs
